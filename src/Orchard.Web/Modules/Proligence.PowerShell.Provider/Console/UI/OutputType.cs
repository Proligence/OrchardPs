namespace Proligence.PowerShell.Provider.Console.UI {
    public enum OutputType {
        Line,
        Warning,
        Error,
        Debug,
        Verbose,
        Progress,
        Completion
    }
}